{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "color": { "mode": "palette-classic" } } },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list" } },
      "targets": [
        { "expr": "soc_alert_count", "legendFormat": "{{severity}} - {{status}}" }
      ],
      "title": "Alert Count by Severity & Status",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        { "expr": "rate(soc_api_requests_total[5m])", "legendFormat": "{{method}} {{endpoint}}" }
      ],
      "title": "API Request Rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        { "expr": "histogram_quantile(0.95, rate(soc_sync_duration_seconds_bucket[5m]))", "legendFormat": "{{task_name}}" }
      ],
      "title": "Sync Duration (p95)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "targets": [
        { "expr": "soc_online_agents", "legendFormat": "Online" },
        { "expr": "soc_offline_agents", "legendFormat": "Offline" }
      ],
      "title": "Agent Status",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["soc", "security"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "SOC Dashboard Metrics",
  "uid": "soc-dashboard",
  "version": 1
}
